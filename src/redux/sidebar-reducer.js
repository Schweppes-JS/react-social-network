let initailState = {
  friends: [
    "Alex",
    "Kate",
    "Ilon",
    "John",
    "Stive"
  ]
};

const sidebarReducer = (state = initailState, action) => {

  return state;
}

export default sidebarReducer;